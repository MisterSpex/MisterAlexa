service: misterspex-alexa

frameworkVersion: ">=1.4.0 <2.0.0"

provider:
  name: aws
  runtime: nodejs6.10
  region: us-east-1

functions:
  processor:
    handler: handler.processor
    events:
      - alexaSkill
